---
- name: "Update authorized key file for user: {{ user_name }}"
  authorized_key: 
    user: "{{ user_name }}"
    key: "{{ lookup('file', authorized_keyfile) }}"
  when:
  - authorized_keyfile is defined
  - authorized_keyfile != ""
