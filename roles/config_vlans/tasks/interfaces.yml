---

- name: "Configure VLAN interfaces"
  template:
    src: vlan.j2 
    dest: /etc/sysconfig/network-scripts/ifcfg-{{ ifcfg.device }}
  with_items:
  - '{{ vlans }}'
  loop_control:
    loop_var: ifcfg

