---

- name: "Retrieve template for Identity Provider"
  set_fact:
    id_provider_json: "{{ lookup('template', 'id-providers.json.j2') | to_json }}"

- name: "Requesting Access Token"
  uri:
    url: "https://{{ rh_sso_host }}:{{ rh_sso_port }}/auth/realms/master/protocol/openid-connect/token"
    method: POST
    body_format: form-urlencoded
    body:
      username: "{{ sso_admin_user }}"
      password: "{{ sso_admin_pass }}"
      grant_type: "password"
      client_id: "admin-cli"
    validate_certs: no
  register: rh_sso_token

- name: "Create Identity Provider for Red Hat Single Sign-On"
  uri:
    url: "https://{{ rh_sso_host }}:{{ rh_sso_port }}/auth/admin/realms/master/identity-provider/instances"
    method: POST
    body: "{{ id_provider_json }}"
    body_format: json
    status_code: 201, 409
    headers:
      Authorization: "Bearer {{ rh_sso_token.json.access_token }}"
    validate_certs: no
  register: rh_sso_realm_create
