---

- set_fact:
    url: "{{redirect_url_protocol }}://{{ redirect_url }}"

- uri:
    url: "{{ url }}"
    validate_certs: "{{ redirect_validate_certs | default(true) }}"
  register: redirect_url

- set_fact:
    {"{{ redirect_var_name }}" : "{{ redirect_url.url }}" }
