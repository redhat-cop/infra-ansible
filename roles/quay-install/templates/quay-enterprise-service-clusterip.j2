apiVersion: v1
kind: Service
metadata:
  namespace: {{QUAY_NAMESPACE}}
  name: {{QUAY_SERVICE_NAME}}
spec:
  type: ClusterIP
  ports:
    - protocol: {{QUAY_SERVICE_PROTOCOL}}
      name: {{QUAY_SERVICE_PORT_NAME}}
      port: {{QUAY_SERVICE_PORT}}
      targetPort: {{QUAY_SERVICE_TARGET_PORT}}
  selector:
    quay-enterprise-component: app