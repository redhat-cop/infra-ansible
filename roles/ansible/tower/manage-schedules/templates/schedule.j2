{
    "rrule": "{{ schedule.rrule }}",
    "name": "{{ schedule.name }}",
    "description": "{{ schedule.description | default('') }}",
    "extra_data": {{ schedule.extra_data | default(None) | to_json }},
    "inventory": "{{ schedule.inventory | default() }}",
    "scm_branch": "{{ schedule.scm_branch | default('') }}",
    "job_type": null,
    "job_tags": "",
    "skip_tags": "",
    "limit": "",
    "diff_mode": null,
    "verbosity": null,
    "unified_job_template": "{{ schedule.unified_job_template | default('') }}",
    "enabled": {{ schedule.enabled | default(true) }}"
}
