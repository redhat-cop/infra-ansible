---
- name: "running task dhcp.yml"
  debug:
    msg: "does this this needs to be enabled DRJ"

#- name: 'Copy the dhcp config file to a temp location for validity checking'
#    src: '{{ dhcp_temp_file }}'
#    dest: '{{ temp_new_file }}'
#    validate: 'dhcp -t -cf %s'

#- name: 'Copy and activate the dhcp config file'
#  copy:
#    src: '{{ dhcp_temp_file }}'
#    dest: '/etc/dhcpd/dhcpd.conf'
#    backup: 'yes'
#    validate: 'dhcp -t -cf %s'
#  notify: 'reload dhcp'
