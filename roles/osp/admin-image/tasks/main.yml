---

- name: "Create the image"
  os_image:
    cloud: "{{ item.cloud }}"
    filename: "{{ item.filename }}"
    disk_format: "{{ item.disk_format | default(omit) }}"
    name: "{{ item.name }}"
  with_items:
  - "{{ osp_default_images }}"
