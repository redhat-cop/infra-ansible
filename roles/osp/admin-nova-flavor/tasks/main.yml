---

- name: "Create the flavor"
  os_nova_flavor:
    cloud: "{{ item.cloud }}"
    vcpus: "{{ item.vcpus }}"
    ram: "{{ item.ram }}"
    disk: "{{ item.disk }}"
    name: "{{ item.name }}"
  with_items:
  - "{{ osp_custom_flavors }}"
