---
- name: "Loop over all zones"
  include_tasks: loop-route53-records.yml
  with_items:
    - "{{ zones_records.results }}"
  loop_control:
    loop_var: r53_zone


#- name: "Loop over all zones"
#      include_tasks: rm_zones.yml
#      with_items:
#        - "{{ route53_data }}"
#      loop_control:
#        loop_var: r53_zone