---
- name: "Loop through zone records"
  include_tasks: empty-zone-route53.yml
  with_subelements: 
    - "{{ r53_zone.ResourceRecordSets }}"
    - ResourceRecords
  loop_control:
    loop_var: r53_record