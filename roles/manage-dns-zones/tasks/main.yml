---

- import_tasks: prereq.yml

- include_tasks: named/main.yml
  when:
    - item.named is defined
  with_items:
    -  "{{ dns_data }}"

- include_tasks: route53/main.yml
  when:
    - item.route53 is defined
  with_items:
    -  "{{ dns_data }}"
