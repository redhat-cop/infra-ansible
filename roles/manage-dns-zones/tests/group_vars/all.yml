dns_data:
- view: "private"
  zone: "roletest4.com"
  state: present
  route53:
    aws_access_key: "{{ aws_access_key }}"
    aws_secret_key: "{{ aws_secret_key }}"
    vpc_id: vpc-9dcde6f8
    vpc_region: eu-west-1
  entries:
  - type: A
    hostname: server_a
    ip: 192.168.1.1
    ttl: 400
    state: present
  - type: A
    hostname: server_b
    ip: 192.168.1.2
    ttl: 500
    state: present
- view: "public"
  zone: "roletest3.com"
  state: present
  route53:
    aws_access_key: "{{ aws_access_key }}"
    aws_secret_key: "{{ aws_secret_key }}"
  entries:
  - type: A
    hostname: server_a
    ip: 192.168.1.1
    ttl: 400
    state: present
  - type: A
    hostname: server_b
    ip: 192.168.1.2
    ttl: 500
    state: present
- view: "public"
  zone: "roletest5.com"
  state: present
  route53:
    aws_access_key: "{{ aws_access_key }}"
    aws_secret_key: "{{ aws_secret_key }}"

  
