---

- name: 'Install required packages'
  package:
    name: '{{ item }}'
    state: installed
  loop: "{{ config_chrony_packages }}"

- name: 'Start firewalld'
  service:
    name: "{{ item }}"
    state: started
    enabled: yes
  loop: "{{ config_chrony_services }}"

- name: 'Open Firewall for NTP/Chrony use'
  firewalld:
    service: "{{ item }}"
    permanent: yes
    state: enabled
    immediate: yes
  loop: "{{ config_chrony_firewall_services }}"


