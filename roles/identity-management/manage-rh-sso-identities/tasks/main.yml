---

- name: Process Red Hat SSO Identities
# include_tasks: create_users.yml
# loop: "{{ sso_user_params }}"
# loop_control:
#   loop_var: sso_user
  include_tasks: delete_users.yml
  loop: "{{ sso_user_params }}"
  loop_control:
    loop_var: sso_user
  import_tasks: update_users.yml
